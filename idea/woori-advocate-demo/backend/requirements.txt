# Heterogeneous LLM ensemble (iter-0002 §1) + ZeroClaw-style multi-auth fallback:
#
# Provider 폴백 체인 (ZeroClaw `~/.zeroclaw/config.toml` 패턴 참고):
#   1. API key (env)   — 가장 빠름, 가장 안정
#   2. CLI OAuth       — OAuth 세션 활용 (claude / codex CLI)
#   3. Mock fallback   — 시연 안정성
#
#   Seller   → AnthropicProvider:
#       API: ANTHROPIC_API_KEY                →  CLI: `claude -p`   →  mock
#   Advocate → OpenAIProvider:
#       API: OPENAI_API_KEY                   →  CLI: `codex exec`  →  mock
fastapi>=0.115.0
httpx>=0.27.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
z3-solver>=4.13.0
